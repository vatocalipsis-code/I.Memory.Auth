# I.Memory.Auth

Public static OAuth consent frontend for ISKRA Memory.

Canonical source lives in the private `vatocalipsis-code/I.Memory` repository under `web/auth/`.

This repository contains no secrets. The Supabase project URL and publishable key are public client configuration.
